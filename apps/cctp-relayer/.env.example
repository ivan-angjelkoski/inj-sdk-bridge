# CCTP Relayer Environment Variables

# Required: Private key of the relayer wallet (32-byte hex string with 0x prefix)
# This wallet will pay gas fees for mint transactions on all destination chains
PRIVATE_KEY=0x...

# Required: Alchemy API key for RPC access
ALCHEMY_API_KEY=your_alchemy_api_key_here

# Optional: Port to run the server on (default: 3001)
PORT=3001
